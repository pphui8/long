[Unit]
Description=Long App Gin Web Server
After=network.target

[Service]
Type=simple
User=admin
# Ensure this directory exists on your server
WorkingDirectory=/code
# Full path to the binary produced by go build
ExecStart=/code/long
Restart=always
RestartSec=5
# environment variables
Environment=GIN_MODE=release
Environment=GEMINI_API=

[Install]
WantedBy=multi-user.target
